// Intentionally empty — actual routing handled by src/app/page.tsx (root)
// and src/app/[...mdxPath]/page.tsx (required catch-all for sub-paths).
// This optional catch-all file exists only because the root app/ directory
// cannot be modified. It has no active exports to avoid conflicts.
export const _unused = true
